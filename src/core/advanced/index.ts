/**
 * The advanced line is not written yet.
 *
 * What it will be is fixed in dev/CHALLENGE.md §9: a risk scan and an authority check
 * that both run on the record layer before any model call, then classification,
 * gate, draft and policy validation as separate passes. The pieces it will be built
 * from are already here — `../authority.ts`, `../policy.ts`, and the prompts beside
 * this file — waiting for the line that arranges them.
 *
 * Until then nothing is exported, `PIPELINES` carries the baseline alone, and the
 * cross-line half of dev/contracts/FEATURE-PARITY.md is suspended in the open.
 */
export {};
